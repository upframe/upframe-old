{{ define "title" }}Invalid confirmation &bull; Upframe{{ end}}
{{ define "content" }}
<main role="main" class="covered">
    <div class="floating-box">
        <h1>Confirmation</h1>
        <p>The link is invalid or it has expired.</p>
    </div>
</main>
{{ end }}

import { h } from 'preact';
import style from './style';

const Apply = () => (
  <div class={style.home}>
    <h1>Apply</h1>
    <p>This is the Apply component.</p>
  </div>
);

export default Apply;